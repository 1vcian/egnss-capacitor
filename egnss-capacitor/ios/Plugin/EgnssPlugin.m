#import <Foundation/Foundation.h>
#import <Capacitor/Capacitor.h>

// The Swift file declares the plugin via `CAPBridgedPlugin`.
// This file is kept for legacy ObjC tooling and to hint the pod at
// an Objective-C presence; no macros are needed since registration
// happens through `pluginMethods` in Swift.
